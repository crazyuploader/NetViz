[project]
name = "netviz"
version = "0.1.0"
description = "Visualize network related data from PeeringDB"
readme = "README.md"
requires-python = ">=3.14.2"
dependencies = [
    "black>=25.12.0",
    "flask>=3.1.2",
    "gunicorn>=23.0.0",
    "pylint>=4.0.4",
    "requests>=2.32.5",
]
